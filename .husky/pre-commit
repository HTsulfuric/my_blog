echo "Running pre-commit hooks..."

npm run update-usage

git add src/data/usage.json
echo "Verified usage data."

npm run lint

if [ $? -ne 0 ]; then
  echo "Linting failed. Please fix the errors before committing."
  exit 1
fi
